##### 1.内存分区

类放在哪个分区？新建一个对象放在哪个分区？多个线程创建对象时时怎样解决冲突的(比如不能放在同一块内存)？

##### 2.垃圾回收

年轻代回收会暂停用户线程嘛(STW)？为什么？从年轻代使用的是“标记-复制”角度着手。

##### 3. 类加载

类加载的步骤？—没看过源码就从应用角度主动说说对开发的影响。

##### 4. 线程交互

一个线程怎样暂停另外一个线程-interrupted及相关方法？线程池的shutdownNow()方法为什么可以立即停止所有线程，而shutdown()不可以？volltile线程安全的使用场景：（只有一个线程写）或者（1.  对其写操作不依赖前值，或者使用CAS实现操作的原子性；2.没有包含在其他变量的不变式中）;面试官可查看的博客记录内容，比如CopyOnWriteList;

##### 5.Spring

Spring的切面编程实现原理—java的动态代理，局限性；Class.forName(string)源码—数据库中类加载的目的和实现。

##### 6.算法题

对一个数组进行预处理后，可以进行O(1)复杂度查询其某个区间的所有元素和——动态规划，O(n)遍历一遍后得到res[n][n]的数组记录了结果。